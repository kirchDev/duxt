---
title: Playground
description: The local, unversioned case — no config at all.
---

# Playground

This file is served by the layer's own `docs` collection. Nothing in
`playground/nuxt.config.ts` configures it: the collection ships with the layer,
and Content merges the config from every layer in the chain.
