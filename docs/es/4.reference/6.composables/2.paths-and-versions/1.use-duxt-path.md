---
title: useDuxtPath()
description: La ruta en la que está la documentación, sin el locale.
icon: lucide:milestone
---

## Firma

```ts
const path = useDuxtPath();
```

## Devuelve

La ruta actual con el segmento de locale eliminado — la ruta tal como la conoce
la documentación.

## Notas

Todo lo que razona sobre páginas razona sobre esto, no sobre `route.path`:
`/de/duxt/getting-started` y `/duxt/getting-started` son la misma página.
