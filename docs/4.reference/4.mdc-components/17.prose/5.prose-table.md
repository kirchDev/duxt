---
title: ProseTable
description: A table that scrolls instead of the page.
icon: lucide:table
---

Every Markdown table.

## What it adds

A scroll container around the table, so a wide table scrolls inside its own box
rather than making the whole page scroll sideways.

## API

### Slots

| Slot      | What goes in it |
| :-------- | :-------------- |
| `default` | The table       |
