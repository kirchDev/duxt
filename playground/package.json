{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "description": "Development playground for the duxt layer — deliberately carries the ugly cases.",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "prepare": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^3.16.0",
    "nuxt": "^4.5.2"
  }
}
