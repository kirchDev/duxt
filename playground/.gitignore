# Nuxt build artefacts and Content's git-source cache. Kept local to the
# playground because the root .gitignore is generated by gitignore-sync and
# carries no nuxt stack.
.nuxt
.output
.data
.nitro
.cache
